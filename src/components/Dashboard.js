import React from 'react';


const Dashboard = () => {
    return (
        <div>
            <h1>Dashboard</h1>
            {/* Add summary of key metrics or features */}
            
            {/* Add links/buttons for quick navigation */}
        </div>
    );
};

export default Dashboard;